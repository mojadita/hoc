#!hoc
int n = 100;
func int gauss(int a, int b, int c) {
    while (b <= a) {
        c = c + b;
        b = b + 1;
    }
    return c;
}
{
    list;
    print "gauss(", n, ") -> ", gauss(n, 0, 0), "\n";
    print n, "*(", n, " + 1)/2 -> ", n*(n + 1)/2, "\n";
}
